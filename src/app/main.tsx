import { Component } from "react";

import './main.scss';

export default class Main extends Component<{}, {}> {
    render() {
        return (
            <div className="main">
                    This is react typescipt app.
            </div>
        );
    }
}